bootstrap_basic_image_gallery.settings:
  type: config_object
  label: 'Bootstrap Basic Image Gallery settings'
  mapping:
    prevent_load_bootstrap:
      type: boolean
      label: 'Prevent module loading Bootstrap scripts'
